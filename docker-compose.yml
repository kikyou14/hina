services:
  hina:
    image: ghcr.io/kikyou14/hina:latest
    container_name: hina
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    stop_grace_period: 15s
    restart: unless-stopped
